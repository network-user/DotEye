# DotEye - runtime зависимости
# Верхние границы держат проверенные major-версии (ABI/API), нижние - не ниже
# веток без известных advisory. Обновлять осознанно, перепроверяя тесты.
aiogram>=3.4
python-dotenv>=1.0
opencv-python>=4.8,<5.0
numpy>=1.24,<2.0
cryptography>=43.0,<51

# Детекция (вариант A, точнее всего): YOLO
ultralytics>=8.1

# Опционально: распознавание лиц (identity mode)
insightface>=0.7.3
onnxruntime>=1.16

# Голос и тревога (SAPI на Windows, espeak на Linux)
pyttsx3>=2.90
pywin32>=306; sys_platform == 'win32'

# Для разработки и тестов см. requirements-dev.txt
